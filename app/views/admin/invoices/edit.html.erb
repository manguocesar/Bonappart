<div class="col-lg-12 mt-8">
  <div class="card mt-3">
    <div class="card-body">
      <div class="row mt-3">
        <div class="col-sm-12">
          <h4 class="header-title"><%= t('invoice.edit_invoice') %></h4>
          <%= render 'form', invoice: @invoice, url: admin_invoice_path, method: :put %>
        </div>
      </div>
    </div>
  </div>
</div>
