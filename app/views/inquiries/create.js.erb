<% if @inquiry.errors.present? %>
  $("#inuiryModal").find(".modal-content").html("<%= j (render 'new') %>");
  $("#inuiryModal").modal();
<% else %>
  $("#inuiryModal").modal('toggle');
  location.reload();
  window.scrollTo(0, 0)
<% end %>
