<%= render 'shared/header_bg' %>
<div class="col-lg-12 mt-8">
  <div class="card mt-3">
    <div class="card-body">
      <div class="row mt-3">
        <div class="col-sm-12">
          <h4 class="header-title"><%= t('apartment.edit_apartment') %></h4>
          <hr>
          <%= render 'form', apartment: @apartment, url: show_page_apartments %>
        </div>
      </div>
    </div>
  </div>
</div>
<%= render '/shared/modal_window', locals: { modelId: 'subscriptionModal' } %>
