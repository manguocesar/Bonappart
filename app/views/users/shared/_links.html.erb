<%- if controller_name != 'sessions' %>
  <p class='text-muted'>
    Have an account?
    <%= link_to "Sign in", popup_forms_path(name: 'login'), remote: true %>
  </p>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <p class='text-muted'>
    Don't have an account?
    <%= link_to "Sign up", popup_forms_path(name: 'student'), remote: true %>
  </p>
<% end %>

<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <p class='text-muted'>
    <%= link_to "Forgot Your Password?", popup_forms_path(name: 'forgot_password'), remote: true %>
  </p>
<% end %>

<%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
  <p class='text-muted'>
    <%= link_to "Didn't receive confirmation instructions?", popup_forms_path(name: 'resend_confirmation'), remote: true %>
  </p>
<% end %>

<%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
  <%= link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name) %><br />
<% end %>

<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider) %><br />
  <% end %>
<% end %>
