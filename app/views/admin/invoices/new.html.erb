<div class="col-lg-12 mt-8">
  <div class="card mt-3">
    <div class="card-body">
      <div class="row mt-3">
        <div class="col-sm-12">
          <h4 class="header-title"><%= t('invoice.add_invoice') %></h4>
          <%= render 'form', invoice: @invoice, url: admin_invoices_path, method: :post %>
        </div>
      </div>
    </div>
  </div>
</div>
