<% if page.current? %>
  <li class="at-active">
    <%= content_tag :a, page, data: { remote: remote } %>
  </li>
<% else %>
  <li>
    <%= link_to page, url, remote: remote, rel: page.rel %>
  </li>
<% end %>
