<%= render 'shared/header_bg' %>
<div class="col-lg-12 mt-8">
  <div class="card mt-3">
    <div class="card-body">
      <div class="row mt-3">
        <div class="col-sm-12">
          <h4 class="header-title"><%= t('apartment.add_apartment') %></h4>
          <%= render partial: 'form', invoice: @invoice, url: invoices_path %>
        </div>
      </div>
    </div>
  </div>
</div>
